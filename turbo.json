{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": [
    ".env"
  ],
  "globalEnv": ["DBNAME", "HOST", "PASSWORD", "PORT", "DBUSER", "DEBUG", "NODE_ENV", "LOGGING_WEBHOOK", "CLIENT_TOKEN", "ERROR_CHANNEL", "VERCEL_URL", "WEBPACK"],
  "pipeline": {
    "start": {
      "outputs": [],
      "dotEnv": [".env"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "dotEnv": [".env"]
    }
  }
}
