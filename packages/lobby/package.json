{
  "name": "@ei/lobby",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "lint:typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "@ei/tsconfig": "*",
    "eslint": "^8.51.0",
    "typescript": "^5.5.2",
    "vitest": "^1.6.1"
  },
  "dependencies": {
    "emoji-regex": "^10.3.0",
    "zod": "^3.22.4"
  }
}
